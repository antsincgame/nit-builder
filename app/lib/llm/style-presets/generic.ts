import type { StylePreset } from "./types";

/**
 * Generic — БАЗОВЫЙ уровень качества для запросов без явной эстетики.
 *
 * Раньше был no-op (пустой addon) — и голый запрос «сделай сайт» уходил в
 * Coder без какого-либо дизайн-направления. Слабая локальная модель (7-9B) в
 * таком вакууме выдаёт generic-Tailwind лендинг 2015 года. Теперь generic
 * несёт универсальный современный премиум-язык (типографика, воздух, иерархия,
 * мягкие поверхности, микро-интерактивность, запрет штампов) — НЕ привязанный
 * к цвету, чтобы палитру по-прежнему задавал color_mood плана. Так даже слабая
 * модель получает чёткую систему, которую остаётся лишь применить, и «наводит
 * красоту» не из собственного вкуса, а из детерминированного направления.
 */
export const GENERIC_PRESET: StylePreset = {
  id: "generic",
  name: "Generic",
  tagline: "Современный премиум",
  description:
    "Базовый стиль для запросов без явной эстетики: чистый современный продукт уровня Linear/Vercel/Stripe, нейтральный по цвету (палитру задаёт план), с щедрым воздухом, ясной типографикой и мягкими поверхностями.",
  available: true,
  tokens: {
    // Палитру намеренно не фиксируем — за цвет отвечает color_mood плана,
    // generic задаёт только визуальный ЯЗЫК, а не конкретные цвета.
    palette: [],
  },
  principles: [
    "Это не generic-шаблон 2015 года, а чистый современный продукт уровня Linear / Vercel / Stripe, нейтральный по цвету.",
    "Щедрый воздух: вертикальные отступы секций clamp(64px, 10vw, 128px), просторный hero.",
    "Ясная типографическая иерархия: заголовок clamp(2.2rem, 5vw, 4rem), line-height 1.05, letter-spacing -0.02em; body 16-18px, абзац ≤ 65ch.",
    "Мягкие скругления 16-24px и тонкие тени 0 10px 30px rgba(15,23,42,.06); без резких чёрных границ.",
    "Карточки приподнимаются на hover: translateY(-4px) + усиленная тень, transition 150-250ms.",
    "Один акцентный цвет последовательно (из плана), остальное нейтральное; максимум два акцента.",
    "Каждая секция несёт конкретный смысл; штампы «Добро пожаловать», «Наши преимущества», «Почему выбирают нас», «О нас» без содержания — запрещены.",
    "Современный гротеск (Inter / Manrope / Geist или системный), подключённый через Google Fonts.",
  ],
  signatureMoves: [
    `:root{--radius:20px;--shadow:0 10px 30px rgba(15,23,42,.06);--shadow-lg:0 24px 60px rgba(15,23,42,.10)}`,
    `.section{padding:clamp(64px,10vw,128px) 24px}`,
    `h1{font-size:clamp(2.2rem,5vw,4rem);line-height:1.05;letter-spacing:-.02em}`,
    `.card{border-radius:var(--radius);box-shadow:var(--shadow);transition:transform .2s ease,box-shadow .2s ease}.card:hover{transform:translateY(-4px);box-shadow:var(--shadow-lg)}`,
    `.cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:28px}`,
  ],
  systemPromptAddon: `
СТИЛЬ: СОВРЕМЕННЫЙ ПРЕМИУМ (универсальный, нейтральный по цвету).

Это базовый уровень качества, а НЕ generic-шаблон: чистый современный продукт уровня Linear / Vercel / Stripe. Цвет берётся из плана (color_mood) — здесь задаётся визуальный язык, а не палитра.

Типографика:
  - Современный гротеск (Inter / Manrope / Geist или системный). Подключи через Google Fonts.
  - Иерархия: заголовок clamp(2.2rem, 5vw, 4rem), line-height 1.05, letter-spacing -0.02em.
  - Body 16-18px, line-height 1.6, ширина абзаца не более 65ch.

Пространство и ритм:
  - Щедрый воздух: вертикальные отступы секций clamp(64px, 10vw, 128px).
  - Просторный hero: крупный заголовок + подзаголовок + один primary CTA, опционально визуал/мокап справа или ниже.
  - Сетка карточек: repeat(auto-fit, minmax(260px, 1fr)), gap 24-32px.

Поверхности:
  - Скругления 16-24px, тонкие тени 0 10px 30px rgba(15,23,42,.06); без резких чёрных границ.
  - Карточки приподнимаются на hover: translateY(-4px) + усиленная тень, transition 150-250ms.

Цвет:
  - Один акцентный цвет последовательно (из плана), остальное нейтральное. Максимум два акцента.

Контент:
  - Каждая секция несёт конкретный бизнес-смысл. ЗАПРЕЩЕНЫ штампы: «Добро пожаловать», «Наши преимущества», «Почему выбирают нас», «О нас» без содержания.
  - Заголовки секций конкретные и про продукт, а не общие.

Микро-детали:
  - Плавные transition на ссылках, кнопках, карточках. CTA заметная, нормальным регистром.
  - focus-visible стили для доступности.

ВАУ-АКЦЕНТ (не делай пресный чёрно-белый сайт):
  - Даже на нейтральный запрос возьми ОДИН живой акцентный цвет. По умолчанию — фирменный градиент indigo→violet→fuchsia (#6366f1 → #8b5cf6 → #d946ef). Если color_mood плана задаёт иное настроение (тёплое, тёмное и т.п.) — следуй ему.
  - primary в палитре — это АКЦЕНТНЫЙ цвет, НЕ чёрный (#0a0a0a). Чёрный/графит оставь только для основного текста.
  - Hero на мягком фоне с глубиной: очень светлый радиальный/меш-градиент из оттенков акцента — НЕ голый белый.
  - Главный заголовок hero — с градиентной заливкой текста (background-clip:text), от тёмного к акценту.
  - Иконки в карточках — в капсулах с акцентным градиентным фоном, не серые.
  - Кнопки CTA — акцентный градиент + мягкая цветная тень; на ховере лёгкий подъём.
  - Карточки — мягкие тени с оттенком акцента, подъём на ховере.
  - Выдели один ключевой элемент (тариф/карточку) акцентным ободком.`,
};
