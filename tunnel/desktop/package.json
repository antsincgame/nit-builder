{
  "name": "@nit/tunnel-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "NIT Tunnel desktop GUI — обёртка над Tauri + React UI для сборки бинарей .dmg/.exe/.AppImage/.deb",
  "scripts": {
    "dev:ui": "npm --workspace @nit/tunnel-desktop-ui run dev",
    "build:ui": "npm --workspace @nit/tunnel-desktop-ui run build",
    "tauri:dev": "npm run dev:ui & cd src-tauri && cargo tauri dev",
    "tauri:build": "npm run build:ui && cd src-tauri && cargo tauri build"
  }
}
